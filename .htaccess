# Server: nginx — questo file è ignorato.
# Il routing delle pagine pubbliche è in /etc/nginx/sites-available/crystaltokyo.it
# Vedi location block per /il-gioco, /ambientazione, /razze, /come-si-gioca.

# Mantenuto nel repo solo come documentazione e per compatibilità
# nel caso il server venga mai migrato ad Apache.
